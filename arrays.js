//Write an algorithm which prints everything in an array.

function arrayElements(a) {
    for (let i = 0; i < a.length; i++) {
      console.log(a[i]);
    }
  }
  
  const container = [2,4,6,8,10];
  arrayElements(container);